'use client'

import { useMemo, useState } from 'react'
import { AlertTriangle, ArrowDownRight, ArrowUpRight, CalendarDays, CheckCircle2, ChevronDown, CircleHelp, Database, MoreHorizontal, Package, Search, SlidersHorizontal, WalletCards } from 'lucide-react'

const demoAlerts = [
  { id: 1, title: 'Stock bajo en insumos críticos', category: 'Stock bajo', tone: 'orange', icon: Package, period: '30 jun 2025', condition: '12 referencias por debajo del punto de reposición', data: 'Stock actual: 186 u. · Mínimo operativo: 240 u.', source: 'ERP interno', status: 'Revisar' },
  { id: 2, title: 'Caída inusual de ventas', category: 'Ventas', tone: 'red', icon: ArrowDownRight, period: '24–30 jun 2025', condition: 'Ventas netas 18,6% por debajo del promedio móvil', data: '$62.480 esta semana · Promedio 4 semanas: $76.730', source: 'CRM y facturación', status: 'Atención' },
  { id: 3, title: 'Aumento de gastos operativos', category: 'Gastos', tone: 'violet', icon: WalletCards, period: 'Junio 2025', condition: 'Gastos 11,2% por encima del período anterior', data: '$18.420 registrados · Mayo 2025: $16.560', source: 'Contabilidad interna', status: 'Revisar' },
  { id: 4, title: 'Inconsistencia en registros', category: 'Inconsistencias', tone: 'blue', icon: Database, period: '29 jun 2025', condition: '14 movimientos sin correspondencia entre sistemas', data: 'ERP: 1.284 movimientos · CRM: 1.270 movimientos', source: 'Cruce ERP / CRM', status: 'Verificar' },
  { id: 5, title: 'Variación histórica relevante', category: 'Histórico', tone: 'yellow', icon: ArrowUpRight, period: '2.º trimestre 2025', condition: 'Producción 9,4% sobre el promedio de los últimos 3 años', data: '3.840 unidades · Promedio histórico: 3.510 unidades', source: 'Producción interna', status: 'Contexto' },
]

const toneMap = {
  orange: { accent: '#ff792c', soft: '#382014', border: '#5a3522' },
  red: { accent: '#ff6b6b', soft: '#3a1d26', border: '#59303b' },
  violet: { accent: '#a998ff', soft: '#241d40', border: '#40356d' },
  blue: { accent: '#55b9ee', soft: '#102b3a', border: '#284b62' },
  yellow: { accent: '#f2c45c', soft: '#382f17', border: '#5a4c25' },
}

function Logo() { return <div className="flex items-center gap-2.5"><div className="grid size-8 place-items-center rounded-lg bg-[#f4511e] shadow-[0_0_18px_rgba(244,81,30,.28)]"><span className="text-lg text-white">✣</span></div><span className="text-[15px] font-bold tracking-tight text-[#f3f5f7]">Stockflow</span></div> }

function SummaryCard({ icon: Icon, label, value, detail, color }) { return <article className="rounded-xl border border-[#293749] bg-[#0f1621] p-4"><div className="flex items-center justify-between"><span className="text-xs text-[#8a98aa]">{label}</span><span className="grid size-8 place-items-center rounded-lg" style={{ color, backgroundColor: `${color}18` }}><Icon size={16} /></span></div><p className="mt-4 text-2xl font-semibold tracking-tight text-[#f3f5f7]">{value}</p><p className="mt-1 text-[11px] text-[#68778b]">{detail}</p></article> }

function AlertRow({ alert }) { const colors = toneMap[alert.tone]; const Icon = alert.icon; return <article className="rounded-xl border border-[#293749] bg-[#0f1621] p-5 transition-colors hover:border-[#3b4c60]"><div className="flex flex-wrap items-start justify-between gap-4"><div className="flex min-w-0 items-start gap-3"><span className="grid size-10 shrink-0 place-items-center rounded-xl" style={{ color: colors.accent, backgroundColor: colors.soft }}><Icon size={19} /></span><div><div className="flex flex-wrap items-center gap-2"><h2 className="text-sm font-semibold text-[#edf2f7]">{alert.title}</h2><span className="rounded-full border px-2 py-0.5 text-[10px] font-medium" style={{ color: colors.accent, borderColor: colors.border, backgroundColor: colors.soft }}>{alert.category}</span></div><p className="mt-2 text-sm text-[#b7c0cc]">{alert.condition}</p></div></div><div className="flex items-center gap-3"><span className="rounded-full border border-[#364457] bg-[#111d2a] px-2.5 py-1 text-[11px] text-[#9eabba]">{alert.status}</span><button aria-label={`Más acciones para ${alert.title}`} className="grid size-8 place-items-center rounded-lg text-[#718096] hover:bg-[#1a2533] hover:text-white"><MoreHorizontal size={17} /></button></div></div><div className="mt-5 grid gap-3 border-t border-[#202c3b] pt-4 sm:grid-cols-3"><div><p className="text-[10px] uppercase tracking-[.12em] text-[#68778b]">Período</p><p className="mt-1 text-xs text-[#d4dce5]">{alert.period}</p></div><div><p className="text-[10px] uppercase tracking-[.12em] text-[#68778b]">Datos de respaldo</p><p className="mt-1 text-xs text-[#d4dce5]">{alert.data}</p></div><div><p className="text-[10px] uppercase tracking-[.12em] text-[#68778b]">Origen</p><p className="mt-1 text-xs text-[#d4dce5]">{alert.source}</p></div></div></article> }

export default function Page({ dataset }) {
  const [query, setQuery] = useState('')
  const [category, setCategory] = useState('Todas')
  const [period, setPeriod] = useState('Últimos 30 días')
  const alerts = useMemo(() => {
    if (!dataset) return demoAlerts
    const results = []
    if (dataset.rejected > 0) results.push({ id: 'rejected', title: 'Registros para revisar', category: 'Inconsistencias', tone: 'orange', icon: Database, period: 'Archivo cargado', condition: `${dataset.rejected} de ${dataset.total} registros quedaron observados`, data: `${dataset.valid} aceptados · ${dataset.rejected} rechazados`, source: dataset.file?.name || 'CSV local', status: 'Revisar' })
    if (dataset.insights.amountChange !== null) {
      const rising = dataset.insights.amountChange >= 0
      results.push({ id: 'amount-change', title: rising ? 'Importes más altos en la segunda mitad' : 'Importes más bajos en la segunda mitad', category: 'Ventas', tone: rising ? 'blue' : 'red', icon: rising ? ArrowUpRight : ArrowDownRight, period: 'Mitades del archivo', condition: `Variación agregada de ${Math.abs(dataset.insights.amountChange)}% entre ambas mitades`, data: `${dataset.total} filas recibidas · comparación según el orden del CSV`, source: dataset.file?.name || 'CSV local', status: 'Contexto' })
    }
    return results
  }, [dataset])
  const filtered = useMemo(() => alerts.filter((alert) => (category === 'Todas' || alert.category === category) && `${alert.title} ${alert.condition} ${alert.data} ${alert.source}`.toLowerCase().includes(query.toLowerCase())), [alerts, category, query])
  return <main className="min-h-screen bg-[#090d15] text-[#f3f5f7]"><header className="border-b border-[#202b39] bg-[#0a0f18]/95"><div className="mx-auto flex h-[66px] max-w-[1180px] items-center justify-between px-5 lg:px-8"><Logo /><nav className="hidden items-center gap-8 text-sm text-[#8792a4] md:flex"><a className="text-[#e8edf3]" href="#alertas">Alertas</a><a className="hover:text-white" href="#historial">Historial</a><a className="hover:text-white" href="#fuentes">Fuentes</a></nav><div className="flex items-center gap-4"><span className="hidden text-sm text-[#8f9bac] sm:block">Hola, Martina</span><button aria-label="Abrir ayuda" className="grid size-8 place-items-center rounded-full border border-[#293546] text-[#8490a3]"><CircleHelp size={16} /></button><div className="grid size-8 place-items-center rounded-full bg-[#293546] text-xs font-semibold text-[#dbe2ea]">ML</div></div></div></header><div className="mx-auto max-w-[1180px] px-5 py-8 lg:px-8 lg:py-10"><div className="mb-8 flex flex-wrap items-end justify-between gap-5"><div><p className="mb-2 text-xs font-medium uppercase tracking-[.18em] text-[#ff792c]">Monitoreo operativo</p><h1 className="text-2xl font-bold tracking-tight sm:text-[30px]">Panel de alertas</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-[#8e9aac]">{dataset ? 'Alertas simuladas desde los registros locales cargados. La comparación sigue el orden del archivo.' : 'Ejemplos de condiciones que pueden detectarse en datos internos.'}</p></div><div className="flex items-center gap-2"><div className="relative"><CalendarDays className="pointer-events-none absolute left-3 top-1/2 -translate-y-1/2 text-[#7f8da0]" size={15} /><select aria-label="Seleccionar período" value={period} onChange={(event) => setPeriod(event.target.value)} className="h-10 appearance-none rounded-lg border border-[#334154] bg-[#0f1621] pl-9 pr-9 text-sm text-[#dce3eb] outline-none"><option>Últimos 30 días</option><option>Último trimestre</option><option>Último año</option></select><ChevronDown className="pointer-events-none absolute right-3 top-1/2 -translate-y-1/2 text-[#7f8da0]" size={15} /></div><button className="inline-flex h-10 items-center gap-2 rounded-lg bg-[#f4511e] px-4 text-sm font-semibold text-white shadow-[0_0_18px_rgba(244,81,30,.18)] hover:bg-[#ff5f28]"><SlidersHorizontal size={15} />Configurar</button></div></div><section id="alertas" className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><SummaryCard icon={AlertTriangle} label="Alertas activas" value={dataset ? String(alerts.length) : '12'} detail={dataset ? `${dataset.rejected} requieren revisión` : '3 requieren atención'} color="#ff792c" /><SummaryCard icon={Database} label="Aceptados" value={dataset ? String(dataset.valid) : '186'} detail={dataset ? `de ${dataset.total} registros` : 'registros de muestra'} color="#55b9ee" /><SummaryCard icon={ArrowDownRight} label="Variación del importe" value={dataset?.insights.amountChange === null ? 'Sin dato' : dataset ? `${dataset.insights.amountChange > 0 ? '+' : ''}${dataset.insights.amountChange}%` : '3'} detail={dataset ? 'entre mitades del CSV' : 'vs. promedio móvil'} color="#ff6b6b" /><SummaryCard icon={CheckCircle2} label="Revisados" value={dataset ? '0' : '8'} detail={dataset ? 'sin acciones registradas' : 'en el período seleccionado'} color="#55b9ee" /></section><section className="mt-8 rounded-xl border border-[#293749] bg-[#0d141e] p-4"><div className="flex flex-wrap items-center gap-3"><div className="relative min-w-[220px] flex-1"><Search className="pointer-events-none absolute left-3 top-1/2 -translate-y-1/2 text-[#6f7d90]" size={16} /><input aria-label="Buscar alertas" value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Buscar por condición, dato o fuente" className="h-10 w-full rounded-lg border border-[#334154] bg-[#101a27] pl-10 pr-3 text-sm text-[#e6ebf1] outline-none placeholder:text-[#69788c] focus:border-[#55b9ee]" /></div><div className="flex items-center gap-2 overflow-x-auto">{['Todas', 'Stock bajo', 'Ventas', 'Gastos', 'Inconsistencias', 'Histórico'].map((item) => <button key={item} onClick={() => setCategory(item)} className={`whitespace-nowrap rounded-lg border px-3 py-2 text-xs font-medium transition-colors ${category === item ? 'border-[#f4511e] bg-[#3a2118] text-[#ff9a72]' : 'border-[#334154] text-[#8997a9] hover:border-[#52647a] hover:text-white'}`}>{item}</button>)}</div></div></section><section className="mt-5 space-y-3" aria-live="polite">{filtered.map((alert) => <AlertRow key={alert.id} alert={alert} />)}{filtered.length === 0 && <div className="rounded-xl border border-dashed border-[#334154] px-6 py-12 text-center text-sm text-[#8290a2]">No hay alertas que coincidan con los filtros seleccionados.</div>}</section><div className="mt-6 flex items-start gap-3 rounded-xl border border-[#4b3e20] bg-[#211c13] p-4 text-xs leading-5 text-[#c7b77f]"><AlertTriangle className="mt-0.5 shrink-0 text-[#f2c45c]" size={16} /><p>Las alertas describen condiciones detectadas y no constituyen diagnósticos automáticos. Confirma los datos de respaldo y considera el contexto antes de actuar.</p></div></div></main>
}
